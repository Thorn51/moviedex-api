{
  "name": "03_moviedex-api",
  "version": "1.0.0",
  "description": "Thinkful Assignment - create an Express application that supports getting movies from a supplied json file and can be filtered by genre, country or average vote.",
  "main": "index.js",
  "scripts": {
    "start": "node server",
    "dev": "nodemon server",
    "predeploy": "npm audit",
    "deploy": "git push heroku master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "10.16.0"
  },
  "author": "Matt Thornberry",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^3.21.2",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
